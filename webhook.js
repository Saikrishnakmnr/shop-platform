import crypto from 'crypto';export default function handler(req,res){if(req.method!=='POST')return res.status(405).end();let raw=typeof req.body==='string'?req.body:JSON.stringify(req.body),sig=req.headers['x-razorpay-signature']||'',e=crypto.createHmac('sha256',process.env.RAZORPAY_WEBHOOK_SECRET).update(raw).digest('hex');if(sig!==e)return res.status(400).json({error:'Invalid webhook'});res.status(200).json({received:true})}
