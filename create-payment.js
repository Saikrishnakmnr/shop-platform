export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{let{amount,receipt}=req.body||{};let auth=Buffer.from(`${process.env.RAZORPAY_KEY_ID}:${process.env.RAZORPAY_KEY_SECRET}`).toString('base64');let r=await fetch('https://api.razorpay.com/v1/orders',{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Basic ${auth}`},body:JSON.stringify({amount:Math.round(Number(amount)*100),currency:'INR',receipt:receipt||`shop_${Date.now()}`})});let d=await r.json();if(!r.ok)return res.status(r.status).json(d);res.json({order:d,keyId:process.env.RAZORPAY_KEY_ID})}catch(e){res.status(500).json({error:e.message})}}
