import crypto from 'crypto';export default function handler(req,res){if(req.method!=='POST')return res.status(405).end();let{razorpay_order_id,razorpay_payment_id,razorpay_signature}=req.body||{};let e=crypto.createHmac('sha256',process.env.RAZORPAY_KEY_SECRET).update(`${razorpay_order_id}|${razorpay_payment_id}`).digest('hex');res.status(200).json({verified:!!razorpay_signature&&crypto.timingSafeEqual(Buffer.from(e),Buffer.from(razorpay_signature))})}
